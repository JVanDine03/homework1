What is the difference between a merge and a rebase?
A merge will update the master and keep the history of before updating the master, while the rebase will rewrite the history to have always had the update master thereby making it one branch instead of two.